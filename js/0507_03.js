const pets = ["dog", "cat", "rabbit", "parret"];
const addPets = ["hamster", "snake"];
const stringPets = pets + addPets;
console.log(stringPets, typeof stringPets);

const newPets = pets.concat(addPets);
console.log(newPets, typeof newPets);

const users = ["jack", "jeni"];
const addUsers = ["sena", "julia"];

const newUsers = users.concat(addUsers);
console.log(newUsers);
